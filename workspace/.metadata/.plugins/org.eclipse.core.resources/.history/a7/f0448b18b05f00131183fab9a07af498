import decimal
from decimal import *
timer=100
x=1*timer
a=x
b=(1/pow(2, 0.5))*timer
c=0.25*timer
for i in range(100):
    y=a
    a=((a+b)/2)
    b=(pow(b*y, 0.5))
    c=(c-x*(pow(a-y, 2)))
    x=(2*x)
    print pow(a+b, 2)/(4*c)