import decimal
from decimal import *
x=1
a=x
b=Decimal(1/pow(2, 0.5))
print b
c=0.25

for i in range(100):
    y=Decimal(a)
    a=Decimal((a+b)/2)
    b=Decimal(pow(b*y, 0.5))
    c=Decimal(c-x*(pow(a-y, 2)))
    x=Decimal(2*x)
    print Decimal(pow(a+b, 2)/(4*c))