import decimal
from decimal import *
timer=10
x=1*timer
a=x
b=1/pow(2, 0.5)
print b
c=0.25
for i in range(100):
    y=a
    a=((a+b)/2)*timer
    b=(pow(b*y, 0.5))*timer
    c=(c-x*(pow(a-y, 2)))*timer
    x=(2*x)
    print pow(a+b, 2)/(4*c)